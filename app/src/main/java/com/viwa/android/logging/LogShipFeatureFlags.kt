package com.viwa.android.logging

/** Client-side gate for periodic app-log shipping (server hello capability optional). */
object LogShipFeatureFlags {
    const val FEATURE_LOG_SHIP: Boolean = true
}
