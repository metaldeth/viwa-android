package com.viwa.android.data.remote.telemetry.mvp.offline

/** Feature gate — server hello `featureFlags.technicianKeys` must also be true. */
object TechnicianKeyFeatureFlags {
    const val FEATURE_TECHNICIAN_KEYS: Boolean = true
}
