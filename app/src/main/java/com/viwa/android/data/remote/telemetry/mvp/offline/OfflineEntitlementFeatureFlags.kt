package com.viwa.android.data.remote.telemetry.mvp.offline

/** Feature flags for offline entitlement (Phase 3). */
object OfflineEntitlementFeatureFlags {
    /** Matches backend `FEATURE_OFFLINE_ENTITLEMENT` — false until pilot soak. */
    const val FEATURE_OFFLINE_ENTITLEMENT: Boolean = true
}
